# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Assinatura: man 2 fstatat (a pagina do syscall `fstatat64` esta sob esse nome)
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: fstatat64
ssn: 300
summary: ""
input_args:
  arg0:
    type: "int"
    name: "dirfd"
    description: "Diretorio de referencia para um caminho relativo. AT_FDCWD (-100) usa o diretorio atual."
  arg1:
    type: "const char *"
    name: "pathname"
    description: "Caminho do arquivo, terminado em nulo."
  arg2:
    type: "struct stat *"
    name: "statbuf"
    description: "Recebe a struct stat com os metadados do arquivo."
  arg3:
    type: "int"
    name: "flags"
    description: "Combinacao de flags da chamada."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
