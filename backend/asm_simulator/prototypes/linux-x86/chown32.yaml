# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Assinatura: man 2 chown (a pagina do syscall `chown32` esta sob esse nome)
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: chown32
ssn: 212
summary: ""
input_args:
  arg0:
    type: "const char *"
    name: "pathname"
    description: "Caminho do arquivo, terminado em nulo."
  arg1:
    type: "uid_t"
    name: "owner"
    description: "Novo dono (UID). -1 nao muda."
  arg2:
    type: "gid_t"
    name: "group"
    description: "Novo grupo (GID). -1 nao muda."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
