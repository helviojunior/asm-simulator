# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Sem pagina man 2 para esta chamada: so o numero e conhecido.
function_name: exit_group
ssn: 252
summary: "Encerra TODAS as threads do processo. E o que a libc chama por tras de exit()."
input_args: {}
output_data:
  type: "void"
  name: "none"
  description: "Nao retorna."
