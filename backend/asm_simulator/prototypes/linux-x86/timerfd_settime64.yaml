# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Assinatura: man 2 timerfd_settime (a pagina do syscall `timerfd_settime64` esta sob esse nome)
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: timerfd_settime64
ssn: 411
summary: ""
input_args:
  arg0:
    type: "int"
    name: "fd"
    description: "Descritor de arquivo aberto. 0 stdin, 1 stdout, 2 stderr."
  arg1:
    type: "int"
    name: "flags"
    description: "Combinacao de flags da chamada."
  arg2:
    type: "const struct itimerspec *"
    name: "new_value"
    description: "Novo tempo do timer. Zerar o valor inicial desarma o timer."
  arg3:
    type: "struct itimerspec *"
    name: "old_value"
    description: "Recebe o tempo anterior do timer. NULL descarta."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
