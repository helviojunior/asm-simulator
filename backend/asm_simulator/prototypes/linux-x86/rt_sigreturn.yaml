# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Assinatura: man 2 sigreturn (a pagina do syscall `rt_sigreturn` esta sob esse nome)
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: rt_sigreturn
ssn: 173
summary: ""
input_args:
  arg0:
    type: "..."
    name: "varargs"
    description: "Argumentos variaveis; o formato depende da operacao pedida."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
