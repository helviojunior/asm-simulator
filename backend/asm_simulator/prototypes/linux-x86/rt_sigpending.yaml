# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Assinatura: man 2 sigpending (a pagina do syscall `rt_sigpending` esta sob esse nome)
# Argumentos da SYSCALL crua, da secao "C library/kernel differences".
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: rt_sigpending
ssn: 176
summary: ""
input_args:
  arg0:
    type: "sigset_t *"
    name: "set"
    description: "Conjunto de sinais da operacao."
  arg1:
    type: "size_t"
    name: "sigsetsize"
    description: "Tamanho em bytes do conjunto de sinais (8 no x86-64, 8 no i386). E o argumento que a versao rt_ tem a mais que o wrapper da libc."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
