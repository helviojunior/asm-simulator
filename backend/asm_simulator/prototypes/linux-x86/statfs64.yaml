# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_32.h
# Assinatura: man 2 statfs (a pagina do syscall `statfs64` esta sob esse nome)
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: statfs64
ssn: 268
summary: ""
input_args:
  arg0:
    type: "const char *"
    name: "path"
    description: "Caminho do arquivo, terminado em nulo."
  arg1:
    type: "struct statfs *"
    name: "buf"
    description: "Ponteiro para o bloco de memoria da operacao."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
