# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_64.h
# Assinatura: man 2 pipe2
function_name: pipe2
ssn: 293
summary: "Cria um pipe, com flags (O_CLOEXEC, O_NONBLOCK) na mesma chamada."
input_args:
  arg0:
    type: "int *"
    name: "pipefd"
    description: "Recebe os dois descritores: [0] le, [1] escreve."
  arg1:
    type: "int"
    name: "flags"
    description: "Combinacao de flags da chamada."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
