# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_64.h
# Assinatura: man 2 exit_group
function_name: exit_group
ssn: 231
summary: "Encerra TODAS as threads do processo com o codigo dado. E o que a libc chama por tras de exit()."
input_args:
  arg0:
    type: "int"
    name: "status"
    description: "Codigo de saida do processo. So os 8 bits baixos chegam a quem espera."
output_data:
  type: "void"
  name: "none"
  description: "Nao retorna."
