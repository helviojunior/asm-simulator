# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_64.h
# Assinatura: man 2 pivot_root
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: pivot_root
ssn: 155
summary: ""
input_args:
  arg0:
    type: "const char *"
    name: "new_root"
    description: "Caminho do novo diretorio raiz."
  arg1:
    type: "const char *"
    name: "put_old"
    description: "Onde a raiz antiga passa a ser montada."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
