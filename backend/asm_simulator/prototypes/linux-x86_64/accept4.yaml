# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_64.h
# Assinatura: man 2 accept4
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: accept4
ssn: 288
summary: "Aceita uma conexao, com flags (SOCK_CLOEXEC, SOCK_NONBLOCK) na mesma chamada."
input_args:
  arg0:
    type: "int"
    name: "sockfd"
    description: "Descritor do socket."
  arg1:
    type: "struct sockaddr *"
    name: "addr"
    description: "Endereco na memoria do processo."
  arg2:
    type: "socklen_t *"
    name: "addrlen"
    description: "socklen_t * addrlen."
  arg3:
    type: "int"
    name: "flags"
    description: "Combinacao de flags da chamada."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
