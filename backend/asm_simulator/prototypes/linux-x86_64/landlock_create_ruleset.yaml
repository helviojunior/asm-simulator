# Prototipo de syscall. Ver prototypes/README.md.
# Numero: /usr/include/x86_64-linux-gnu/asm/unistd_64.h
# Assinatura: man 2 landlock_create_ruleset
# TODO: resumo e descricoes derivados da assinatura; revisar.
function_name: landlock_create_ruleset
ssn: 444
summary: ""
input_args:
  arg0:
    type: "const struct landlock_ruleset_attr *"
    name: "attr"
    description: "Estrutura de atributos da operacao."
  arg1:
    type: "size_t"
    name: "size"
    description: "Tamanho em bytes da estrutura passada — e como o kernel sabe a versao dela."
  arg2:
    type: "uint32_t"
    name: "flags"
    description: "Combinacao de flags da chamada."
output_data:
  type: "long"
  name: "result"
  description: "Valor de retorno; negativo em erro (-errno)."
